# .env.example - Template for Maximo Identity Testing Toolkit Configuration

# --- Keycloak Passwords ---
# IMPORTANT: Change these to strong, unique passwords when creating your .env file.
# Keep these secrets safe and do not commit them to public version control.

# Password for the Keycloak admin user (default username is 'admin')
KEYCLOAK_ADMIN_PASSWORD=change_me_to_a_strong_password_for_keycloak_admin

# --- PostgreSQL Database for Keycloak ---
# Default PostgreSQL user and DB name are 'keycloak'.
# Ensure this password matches the one in the docker-compose.yml for Keycloak DB connection.
PG_PASSWORD=change_me_to_a_strong_password_for_postgres

# --- Keycloak Hostname & Port ---
# The URL users will use to access the Keycloak Admin Console and for SAML/OIDC flows.
# Use 'https://localhost:8443' if running locally and using mkcert.
# If Keycloak is hosted on a domain, use that domain (e.g., 'https://keycloak.mycompany.com:8443').
KEYCLOAK_HOSTNAME_URL=https://localhost:8443

# The hostname part, often needed for Keycloak's internal configuration.
# Usually derived from KEYCLOAK_HOSTNAME_URL.
KEYCLOAK_HOSTNAME=localhost

# Port Keycloak listens on for HTTPS
KC_HTTPS_PORT=8443

# Optional: Disable Keycloak's HTTP listener if you only want HTTPS
# KC_HTTP_ENABLED=false

# Optional: Control strict HTTPS hostname validation (set to false for development flexibility)
# KC_HOSTNAME_STRICT_HTTPS=false

# --- OpenLDAP Configuration (Optional, if included in your setup) ---
# IMPORTANT: Change these passwords if you are using the OpenLDAP service.

# Admin credentials for OpenLDAP
LDAP_ADMIN_PASSWORD=change_me_to_a_strong_password_for_ldap_admin
# Replica password for OpenLDAP replication setup
LDAP_REPL_PASSWORD=change_me_to_a_strong_password_for_ldap_replica

# LDAP Base DN and Domain
LDAP_BASE_DN=dc=example,dc=com
LDAP_DOMAIN=example.com

# --- End of Configuration ---