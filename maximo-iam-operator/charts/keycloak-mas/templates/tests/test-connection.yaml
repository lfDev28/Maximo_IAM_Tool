apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-mas.fullname" . }}-test-connection"
  labels:
    {{- include "keycloak-mas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-mas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
